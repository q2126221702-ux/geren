## 题域引擎 1.0.2

### 新增

- “我的 → 设置”新增检查更新功能
- 支持从 GitHub Releases 下载并调用系统安装器完成覆盖升级
- 下载完成后校验 APK 必须使用与当前应用相同的正式签名

### 修复

- 修复长题干或选项较多时，最后一个选项被底部操作栏遮挡的问题
- 答题内容区现在可以独立滚动，切换题目会自动回到顶部

### 安装说明

本次需手动安装 `app-release.apk`。安装 1.0.2 后，后续版本可以在 App 内通过“检查更新”完成升级。直接覆盖安装会保留现有学习记录。
